---
phase: 02-sessao-lista-e-texto-da-marca
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/marcaja/app.py
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03, WM-01, WM-02]
user_setup:
  - "Emulador/dispositivo Android com `briefcase run android` (mesmo fluxo da Fase 1)."
must_haves:
  truths:
    - "Botão **Adicionar PDF ou imagem** abre `OpenFileDialog` com `multiple_select=True` e tipos pdf/png/jpg/jpeg (**D-05**–**D-07**)."
    - "Lista visível com nome + tipo; estado vazio **Nenhum arquivo selecionado** (**D-03**, **D-09**)."
    - "**Remover** por linha funciona (**D-10**)."
    - "Campo **Marca d'água** multilinha, placeholder de exemplo, contador N/200, máx. 200 bloqueado (**D-04**, **D-13**–**D-16**)."
    - "**Ok** valida lista e texto; diálogos pt-BR; sucesso parcial informa próxima etapa (**D-17**–**D-20**)."
    - "**Sair** continua encerrando via `_quit_app`."
  artifacts:
    - src/marcaja/app.py
  key_links:
    - "Usa `Session` de `session.py` como única fonte do lote."
    - "Layout conforme `02-UI-SPEC.md` e tokens herdados de `01-UI-SPEC.md`."
---

<objective>
Integrar **sessão + UI** na MainWindow: picker multi, lista editável, campo de marca com limite 200 e validação do **Ok** sem processar PDF.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-sessao-lista-e-texto-da-marca/02-CONTEXT.md
@.planning/phases/02-sessao-lista-e-texto-da-marca/02-UI-SPEC.md
@.planning/phases/02-sessao-lista-e-texto-da-marca/02-RESEARCH.md
@.planning/phases/02-sessao-lista-e-texto-da-marca/02-PATTERNS.md
@src/marcaja/session.py
@src/marcaja/app.py
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: App Toga + layout Fase 2</name>
  <files>src/marcaja/app.py</files>
  <read_first>
    src/marcaja/app.py,
    .planning/phases/02-sessao-lista-e-texto-da-marca/02-UI-SPEC.md,
    src/marcaja/session.py
  </read_first>
  <action>
    Refatorar para subclasse `MarcajaApp(toga.App)` (nome ajustável) com `startup()`:
    - Instanciar `self.session = Session()`.
    - Construir `MainWindow` com coluna: título **MarcaJá**; botão **Adicionar PDF ou imagem**; área central `flex=1` com `DetailedList` (dados derivados de `session.entries`, accessors title=display_name, subtitle=kind) **ou** `ScrollContainer` com linhas — incluir `Label` *Nenhum arquivo selecionado* quando `session.is_empty()` e lista oculta/vazia conforme UX.
    - Bloco marca: `Label` **Marca d'água**, `MultilineTextInput` com placeholder *Uso exclusivo — João Silva — maio/2026*, `validators=[MaxLength(200)]`, `Label` contador alinhado à direita atualizado em `on_change`.
    - Footer: **Ok** e **Sair** (`_quit_app`).
    - Métodos `_refresh_file_list()` e `_update_counter()` para sincronizar widgets após mudanças na sessão.
    - `main()` retorna instância da subclasse (manter `app_id` existente).
  </action>
  <verify>python -m compileall -q src/marcaja</verify>
  <acceptance_criteria>
    - "`app.py` contém substring `Adicionar PDF ou imagem`."
    - "`app.py` contém `Marca d'água` e `Nenhum arquivo selecionado`."
    - "`app.py` importa e usa `Session`."
  </acceptance_criteria>
  <done>Shell UI montado.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Picker async e remover (FILE-01, FILE-03)</name>
  <files>src/marcaja/app.py</files>
  <read_first>
    .planning/phases/02-sessao-lista-e-texto-da-marca/02-RESEARCH.md,
    src/marcaja/session.py
  </read_first>
  <action>
    Implementar `async def on_add_files(self, widget)`:
    - `await self.main_window.dialog(toga.OpenFileDialog(title=..., file_types=[\"pdf\",\"png\",\"jpg\",\"jpeg\"], multiple_select=True))`.
    - Se resultado `None`, retornar sem mensagem (**D-08**).
    - Normalizar retorno `Path | list[Path]` → `session.add_paths` → `_refresh_file_list()`.
    Implementar remoção: `on_primary_action` do `DetailedList` com `primary_action=\"Remover\"` chamando `session.remove_at` + refresh; ou handler por botão em linha customizada.
    Garantir acumulação em seleções sucessivas (**D-06**).
  </action>
  <verify>python -m compileall -q src/marcaja</verify>
  <acceptance_criteria>
    - "`app.py` contém `OpenFileDialog` e `multiple_select=True`."
    - "`app.py` contém handler async `on_add` ou `on_add_files`."
    - "Remoção referenciada (`Remover` ou `remove_at`)."
  </acceptance_criteria>
  <done>Fluxo de arquivos ligado à sessão.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Marca 200 chars + validação Ok</name>
  <files>src/marcaja/app.py</files>
  <read_first>
    .planning/phases/02-sessao-lista-e-texto-da-marca/02-CONTEXT.md,
    .planning/phases/02-sessao-lista-e-texto-da-marca/02-UI-SPEC.md
  </read_first>
  <action>
    - Sincronizar `session.watermark_text` com o `MultilineTextInput` (on_change); impedir valor &gt;200 caracteres no estado da sessão.
    - `async def on_ok(self, widget)`: se `session.is_empty()` → `ErrorDialog` *Adicione pelo menos um arquivo para continuar.* (ou microcopy equivalente); elif texto vazio/strip → `ErrorDialog` sobre texto obrigatório; else → `InfoDialog` informando que processamento virá na próxima etapa (**D-20**). Usar `await self.main_window.dialog(...)`.
    Manter **Sair** inalterado em comportamento.
  </action>
  <verify>python -m compileall -q src/marcaja</verify>
  <acceptance_criteria>
    - "`app.py` contém `ErrorDialog` e `InfoDialog`."
    - "`app.py` contém `MaxLength` ou validação explícita de 200."
    - "Handler `on_ok` async presente."
  </acceptance_criteria>
  <done>WM-01/02 e validação Ok completos.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall -q src/marcaja`
- [ ] Smoke manual: adicionar arquivos, remover, contador, Ok (3 cenários de diálogo)
</verification>

<success_criteria>
- Critérios de sucesso da Fase 2 no ROADMAP atendidos na UI (verificação humana na UAT)
</success_criteria>
