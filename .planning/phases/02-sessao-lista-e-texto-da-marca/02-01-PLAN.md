---
phase: 02-sessao-lista-e-texto-da-marca
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/marcaja/session.py
  - tests/test_session.py
  - pyproject.toml
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03]
user_setup: []
must_haves:
  truths:
    - "**D-22**: estado do lote centralizado em `session.py`."
    - "Entradas duplicadas (mesmo path resolvido) são ignoradas (**D-11**)."
    - "Cada entrada expõe nome para exibição e tipo PDF/PNG/JPEG (**D-09**)."
    - "`pytest -q tests/test_session.py` passa."
  artifacts:
    - src/marcaja/session.py
    - tests/test_session.py
  key_links:
    - "Extensões permitidas: pdf, png, jpg, jpeg (**D-07**)."
---

<objective>
Implementar o **modelo de sessão** em memória (`session.py`) com acumulação, deduplicação, remoção e classificação de tipo — base para a UI da Fase 2 sem pipeline PDF.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-sessao-lista-e-texto-da-marca/02-CONTEXT.md
@.planning/phases/02-sessao-lista-e-texto-da-marca/02-RESEARCH.md
@.planning/phases/02-sessao-lista-e-texto-da-marca/02-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: session.py — FileEntry e Session</name>
  <files>src/marcaja/session.py</files>
  <read_first>
    .planning/phases/02-sessao-lista-e-texto-da-marca/02-CONTEXT.md,
    .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    Criar `src/marcaja/session.py` com:
    - `ALLOWED_SUFFIXES = {".pdf", ".png", ".jpg", ".jpeg"}` e função `kind_from_path(path) -> str` retornando `"PDF"`, `"PNG"` ou `"JPEG"` (jpg/jpeg → JPEG).
    - `@dataclass` `FileEntry` com `path: Path`, `display_name: str` (basename), `kind: str`.
    - Classe `Session` com lista interna ordenada, propriedade `entries` (cópia ou sequência imutável), `watermark_text: str = ""`.
    - `add_paths(paths: Iterable[Path | str]) -> int` — retorna quantos **novos** foram adicionados; ignorar paths cuja extensão não está em `ALLOWED_SUFFIXES`; ignorar duplicatas por `Path.resolve()` quando possível, senão comparação por string normalizada.
    - `remove_at(index: int) -> None` — levantar `IndexError` se inválido (caller UI trata).
    - `def is_empty(self) -> bool`.
    Não importar Toga neste módulo.
  </action>
  <verify>python -m compileall -q src/marcaja</verify>
  <acceptance_criteria>
    - "`src/marcaja/session.py` define `Session` e `FileEntry`."
    - "`kind_from_path` ou equivalente distingue PDF vs PNG vs JPEG."
  </acceptance_criteria>
  <done>Módulo de sessão importável.</done>
</task>

<task type="auto">
  <name>Tarefa 2: testes unitários da sessão (Wave 0)</name>
  <files>tests/test_session.py, pyproject.toml</files>
  <read_first>src/marcaja/session.py</read_first>
  <action>
    Criar `tests/test_session.py` com pytest cobrindo: (1) `add_paths` acumula dois paths distintos; (2) segundo `add_paths` com mesmo path não aumenta tamanho; (3) `remove_at` remove entrada correta; (4) extensão `.txt` ignorada; (5) `kind` correto para `.pdf` e `.png`. Se `pytest` não estiver em `dev` optional-dependencies, adicionar `pytest>=8` em `[project.optional-dependencies] dev` sem remover entradas existentes.
  </action>
  <verify>pytest -q tests/test_session.py</verify>
  <acceptance_criteria>
    - "`pytest -q tests/test_session.py` retorna código 0."
  </acceptance_criteria>
  <done>Gate automatizado para lógica de lote.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall -q src/marcaja`
- [ ] `pytest -q tests/test_session.py`
</verification>

<success_criteria>
- FILE-01/02/03 suportados em código (camada de dados); UI no plano 02-02
</success_criteria>
