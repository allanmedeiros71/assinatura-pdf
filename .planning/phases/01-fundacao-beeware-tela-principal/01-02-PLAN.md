---
phase: 01-fundacao-beeware-tela-principal
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/marcaja/app.py
  - src/marcaja/resources/fonts/LiberationSans-Regular.ttf
  - README.md
  - .planning/PROJECT.md
autonomous: true
requirements: [UI-01, UI-02, UI-03, WM-07]
user_setup:
  - "Para smoke Android: JDK 17, ANDROID_HOME, `briefcase create android` já executado uma vez no ambiente do desenvolvedor."
must_haves:
  truths:
    - "**D-03**: strings de UI em pt-BR."
    - "**D-04**: uma MainWindow sem onboarding; título MarcaJá; Ok e Sair visíveis na base."
    - "**D-05**: Sair encerra o app via API Toga suportada."
    - "**D-06**: Liberation Sans TTF no repo com menção OFL em README e PROJECT."
    - "Strings visíveis na UI incluem exatamente os rótulos **Ok** e **Sair** na área inferior (UI-03)."
    - "Arquivo `src/marcaja/resources/fonts/LiberationSans-Regular.ttf` existe e tem tamanho > 10 KiB."
    - "`README.md` menciona explicitamente **Liberation Sans** e licença **OFL** (WM-07)."
  artifacts:
    - src/marcaja/resources/fonts/LiberationSans-Regular.ttf
    - src/marcaja/app.py
  key_links:
    - "Layout segue regiões topo / centro / inferior descritas em `01-UI-SPEC.md`."
---

<objective>
Implementar a **tela principal** conforme `01-UI-SPEC.md` e `01-CONTEXT.md` (**D-03**–**D-06**): uma `MainWindow`, título **MarcaJá**, placeholder central, botões **Ok** (primário) e **Sair** (encerra app), tipografia/cores coerentes com Material via Toga; incorporar **Liberation Sans** no repositório; documentar licença/fonte em **README** e tabela **Key Decisions** de `.planning/PROJECT.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundacao-beeware-tela-principal/01-UI-SPEC.md
@.planning/phases/01-fundacao-beeware-tela-principal/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/marcaja/app.py
@README.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: layout MainWindow (UI-01, UI-02, UI-03)</name>
  <files>src/marcaja/app.py</files>
  <read_first>
    src/marcaja/app.py,
    .planning/phases/01-fundacao-beeware-tela-principal/01-UI-SPEC.md,
    .cursor/rules/language-pt-br.mdc
  </read_first>
  <action>
    Refatorar `startup()` para construir: `outer = toga.Box(direction=toga.Column)` com padding consistente com tokens do UI-SPEC (múltiplos de 4 onde API permitir). Topo: `toga.Label` com texto exato `MarcaJá` em tamanho/peso visual de título. Centro: `toga.Label` multilinha com placeholder aprovado no UI-SPEC (ex.: frase **\"Em breve: lote e marca d'água.\"** — ajustar aspas/typographic no código Python). Rodapé: `toga.Box(direction=toga.Row)` com dois `toga.Button`: primeiro **Ok** (handler `on_ok` pode ser `async` ou sync que apenas `pass` ou `print` de debug documentado — sem processamento de PDF). segundo **Sair** chama `self.exit()` ou API suportada pelo Toga 0.5 para encerrar (**D-05**). Garantir que ambos botões permaneçam visíveis em viewport típica (flex do centro = 1 se disponível). Estilizar Ok como ação primária se API Toga expuser `style`/factory; caso contrário, documentar limitação na SUMMARY sem violar visibilidade.
  </action>
  <verify>python -m compileall -q src/marcaja</verify>
  <acceptance_criteria>
    - "`src/marcaja/app.py` contém a string exata `MarcaJá` pelo menos uma vez em rótulo de título."
    - "`src/marcaja/app.py` contém a string exata `Ok` como texto de botão."
    - "`src/marcaja/app.py` contém a string exata `Sair` como texto de botão."
    - "`src/marcaja/app.py` contém chamada a `self.exit` ou `self.app.exit` ou equivalente documentado na doc Toga para fechar (**D-05**)."
  </acceptance_criteria>
  <done>UI principal alinhada ao contrato.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Liberation Sans e documentação WM-07</name>
  <files>src/marcaja/resources/fonts/LiberationSans-Regular.ttf, README.md, .planning/PROJECT.md</files>
  <read_first>
    README.md,
    .planning/PROJECT.md,
    .planning/phases/01-fundacao-beeware-tela-principal/01-CONTEXT.md
  </read_first>
  <action>
    Criar diretório `src/marcaja/resources/fonts/`. Baixar o arquivo **LiberationSans-Regular.ttf** a partir da fonte oficial **fontforge/liberation-fonts** (preferir tag/release **2.1.5**; se URL raw mudar, usar tarball de release e extrair somente esse TTF). Salvar como `src/marcaja/resources/fonts/LiberationSans-Regular.ttf`. Atualizar `README.md`: seção curta **Fontes** citando **Liberation Sans**, licença **SIL Open Font License (OFL)** e caminho do arquivo no repo. Atualizar `.planning/PROJECT.md` na tabela **Key Decisions**: linha da fonte Arial/equivalente com **Outcome** referindo Liberation Sans + OFL + caminho `src/marcaja/resources/fonts/LiberationSans-Regular.ttf`.
  </action>
  <verify>test -s src/marcaja/resources/fonts/LiberationSans-Regular.ttf</verify>
  <acceptance_criteria>
    - "Arquivo `src/marcaja/resources/fonts/LiberationSans-Regular.ttf` existe."
    - "`README.md` contém substring `Liberation` e substring `OFL` ou `Open Font`."
    - "`README.md` contém o caminho `src/marcaja/resources/fonts/` ou nome do arquivo `LiberationSans-Regular.ttf`."
    - "`.planning/PROJECT.md` contém `Liberation` na tabela Key Decisions."
  </acceptance_criteria>
  <done>Fonte empacotada e rastreabilidade WM-07 em docs.</done>
</task>

<task type="auto">
  <name>Tarefa 3: instruções de execução Android (D-02)</name>
  <files>README.md</files>
  <read_first>
    README.md,
    .planning/research/STACK.md
  </read_first>
  <action>
    Atualizar `README.md` na seção de desenvolvimento: comandos **concretos** na ordem lógica — criar venv Python 3.12, `pip install -e ".[dev]"` ou equivalente definido no `pyproject.toml`, `briefcase create android` (primeira vez), `briefcase run android` — com aviso de que JDK 17 e Android SDK são necessários. Mencionar que smoke de UI completo é manual.
  </action>
  <verify>grep -q "briefcase" README.md && grep -q "android" README.md</verify>
  <acceptance_criteria>
    - "`README.md` contém a substring `briefcase run android` ou `briefcase dev android`."
    - "`README.md` menciona Python **3.12** ou `3.12`."
  </acceptance_criteria>
  <done>Fluxo documentado para validação D-02.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall -q src/marcaja`
- [ ] Smoke manual opcional: `briefcase run android` quando SDK disponível
</verification>

<success_criteria>
- Requisitos UI-01, UI-02, UI-03 e WM-07 endereçados no código e artefatos listados
- Nenhuma funcionalidade da Fase 2+ implementada além de stubs explícitos
</success_criteria>
