---
phase: 01-fundacao-beeware-tela-principal
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/marcaja/__init__.py
  - src/marcaja/app.py
autonomous: true
requirements: [PLAT-01]
user_setup:
  - "Python 3.12 em venv (recomendado) com pip atualizado."
must_haves:
  truths:
    - "`python -m compileall -q src/marcaja` termina com código 0."
    - "Decisão **D-01** atendida: app Briefcase com nome/slug `marcaja` e Python 3.12 em `requires-python`."
    - "Decisão **D-02** parcialmente atendida neste plano: base Briefcase pronta; comandos Android ficam documentados no plano **01-02**."
    - "`pyproject.toml` contém `[tool.briefcase.app.marcaja]` com `sources = [\"src/marcaja\"]` e `bundle` reverse-DNS sob `dev.marcaja`."
    - "`src/marcaja/app.py` exporta `main()` que retorna instância de `toga.App` (contrato Briefcase)."
  artifacts:
    - pyproject.toml
    - src/marcaja/app.py
    - src/marcaja/__init__.py
  key_links:
    - "Metadados `[project]` alinham `requires-python` a >=3.10,<3.13 e dependência `toga~=0.5.4` conforme STACK.md."
---

<objective>
Criar o **projeto BeeWare/Briefcase** `marcaja` no repositório: `pyproject.toml` válido, pacote `src/marcaja` e `main()` compatível com Briefcase, cumprindo **PLAT-01** e decisões **D-01** (slug `marcaja`, Python 3.12) e **D-02** (base Android Briefcase documentada, ainda sem exigir build APK nesta tarefa).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-fundacao-beeware-tela-principal/01-CONTEXT.md
@.planning/phases/01-fundacao-beeware-tela-principal/01-RESEARCH.md
@.planning/phases/01-fundacao-beeware-tela-principal/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: pyproject.toml Briefcase + PEP 621</name>
  <files>pyproject.toml</files>
  <read_first>.planning/research/STACK.md</read_first>
  <action>
    Criar `pyproject.toml` na raiz do repositório com: `[build-system]` usando `briefcase==0.4.2` e `setuptools` recente; `[project]` com `name = "marcaja"`, `version = "0.1.0"`, `description` em pt-BR alinhado a MarcaJá, `readme = "README.md"`, `requires-python = ">=3.10,<3.13"`, `dependencies = ["toga~=0.5.4"]`, `[project.optional-dependencies]` com `dev = ["briefcase==0.4.2"]`. Seção `[tool.briefcase]`: `project_name = "MarcaJá"`, `bundle = "dev.marcaja"`, `version = "0.1.0"`, `license = "MIT"`. Seção `[tool.briefcase.app.marcaja]`: `formal_name = "MarcaJá"`, `description` curta, `long_description` apontando para README se suportado, `sources = ["src/marcaja"]`, `requires = ["toga~=0.5.4"]`. Não adicionar PyMuPDF nem Chaquopy nesta fase.
  </action>
  <verify>python -c "import tomllib, pathlib; tomllib.loads(pathlib.Path('pyproject.toml').read_text())"</verify>
  <acceptance_criteria>
    - "Arquivo `pyproject.toml` existe na raiz e parseia como TOML válido."
    - "`pyproject.toml` contém a string literal `[tool.briefcase.app.marcaja]`."
    - "`pyproject.toml` contém `toga~=0.5.4` e `briefcase==0.4.2` (em dev ou build-system)."
    - "`pyproject.toml` contém `bundle = \"dev.marcaja\"` (aspas podem ser simples TOML)."
  </acceptance_criteria>
  <done>TOML Briefcase criado e válido.</done>
</task>

<task type="auto">
  <name>Tarefa 2: pacote src/marcaja e app Toga mínimo</name>
  <files>src/marcaja/__init__.py, src/marcaja/app.py</files>
  <read_first>
    pyproject.toml,
    .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    Criar diretório `src/marcaja/`. Em `__init__.py`, definir `__version__ = \"0.1.0\"` (ou export mínimo). Em `app.py`, implementar subclasse de `toga.App` (ex.: `Marcaja`) cujo `startup()` define `self.main_window = toga.MainWindow(title=self.formal_name)` com `content` sendo um `toga.Box` simples (placeholder textual **pt-BR** permitido). Implementar `def main(): return Marcaja(formal_name=\"MarcaJá\", app_id=\"dev.marcaja.marcaja\", icon=None)` ou assinatura equivalente suportada pela versão Toga instalada — ajustar `app_id` se a API exigir outro parâmetro, mantendo prefixo `dev.marcaja`. Incluir bloco `if __name__ == \"__main__\":` chamando `main().main_loop()` apenas se for o padrão do template BeeWare para desktop; para Android o Briefcase invoca `main()` sem esse bloco — seguir documentação Toga 0.5 se conflitar (prioridade: entrypoint Briefcase funcional).
  </action>
  <verify>python -m compileall -q src/marcaja</verify>
  <acceptance_criteria>
    - "`src/marcaja/app.py` contém `def main(`."
    - "`src/marcaja/app.py` contém a substring `toga.App` ou import de `App` de `toga` com subclasse equivalente."
    - "`python -m compileall -q src/marcaja` retorna código 0."
  </acceptance_criteria>
  <done>Scaffold Python importável e sintaticamente válido.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall -q src/marcaja`
- [ ] `pip install -e .` ou `pip install .` em venv limpo opcional — se executado, deve instalar sem erro (não obrigatório se ambiente offline)
</verification>

<success_criteria>
- Projeto Briefcase/Toga mínimo presente no repositório
- Critérios `must_haves` satisfeitos após execução
</success_criteria>
